
cat Makefile | grep clean | wc -l 

start
	-rediriger l'output vers le pipe (pipe_fd[1])
	dup2(pipe_fd[1], STDOUT_FILENO)
	-si il y a une redirection > || >> on dup2 au dessus
	dup2(fd_outfile, STDOUT_FILENO) 

mid
	-lire depuis le pipe(pipe_fd[0])
	dup2(pipe_fd[0], STDIN_FILENO)
	-rediriger l'output vers le pipe (pipe_fd[1])
	dup2(pipe_fd[1], STDOUT_FILENO)
	-si il y a une redirection > || >> on dup2 au dessus
	dup2(fd_outfile, STDOUT_FILENO)

end
	-lire depuis le pipe(pipe_fd[0])
	dup2(pipe_fd[0], STDIN_FILENO)
	-ne pas rediriger l'output car STDOUT_FILENO est bon cette fois
	-si il y a une redirection > || >> on dup2 au dessus
	dup2(fd_outfile, STDOUT_FILENO)
	