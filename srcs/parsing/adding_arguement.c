#include "../../includes/minishell.h"

int handle_word_arg(t_all *all, int ind, t_cmd *cmd)
{
    //add logic to proceed adding an arguement of one word
}

static t_args   *init_arg(t_all *all)
{
    //add logic for initialization of t_args var
}

int add_arg(t_all *all, t_cmd *cmd, char *arg, int len)
{
    //add logic for adding the arguement value to the cmd
}